plugins {
    id("dev.isxander.multienv.base") // Gives you the most basic functionality to get a working mod environment for desired loaders.

    // TODO: Potential ideas for other extension plugins
    /*
    id("dev.isxander.multienv.publishing") // Adds publishing capabilities to your mod, like modmuss50's mod-publish-plugin and maven.
    id("dev.isxander.multienv.datagen") // Sets up your modding environment for datagen.
     */
}

